import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Parcial2Movil2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
